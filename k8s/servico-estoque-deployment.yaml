apiVersion: apps/v1
kind: Deployment
metadata:
  name: servico-estoque
  namespace: loja-veloz
spec:
  replicas: 2
  selector:
    matchLabels:
      app: servico-estoque
  template:
    metadata:
      labels:
        app: servico-estoque
    spec:
      containers:
        - name: servico-estoque
          image: zPirataa/servico-estoque:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
